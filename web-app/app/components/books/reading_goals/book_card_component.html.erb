<div class="space-y-2">
  <%= render Books::CardComponent.new(book: item.listable, index: index) %>
  <p class="text-sm text-base-content/70">
    Completed <%= item.completed_on.to_fs(:long) %>
  </p>
</div>
