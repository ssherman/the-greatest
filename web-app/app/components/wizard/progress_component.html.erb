<ul class="steps steps-horizontal w-full">
  <% filtered_steps.each_with_index do |step, index| %>
    <li class="step <%= step_status(index) %>" data-content="<%= step_icon(index) %>">
      <span class="text-xs md:text-sm"><%= step[:name].titleize %></span>
    </li>
  <% end %>
</ul>
